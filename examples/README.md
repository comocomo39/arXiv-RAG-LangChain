# Examples

This folder contains curated example outputs from the **BM25 + LLM** search pipeline.

## Available examples

* **Dense retrieval training with hard negatives**
  Query: `papers about dense retrieval training with hard negatives`

* **Learning to rank**
  Query: `retrieval ranking learning to rank`

* **Short ambiguous query (FAISS ANN)**
  Query: `FAISS ANN`

## Notes

* These are example outputs generated from the local project pipeline.
* BM25 performs the primary retrieval.
* The LLM is used for grounded synthesis and query refinement suggestions.
