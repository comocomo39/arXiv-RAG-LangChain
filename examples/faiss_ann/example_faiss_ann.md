# Example Output — FAISS ANN

**User query:** `FAISS ANN`

---

## Queries Used

* FAISS ANN

---

## Retrieved Papers (preview)

### 1) GGNN: Graph-based GPU Nearest Neighbor Search

* **arXiv ID:** `1912.01059`
* **Year:** `None`
* **Categories:** `['cs.CV cs.DB cs.DS cs.IR']`
* **URL:** https://arxiv.org/abs/1912.01059

### 2) An Empirical Comparison of FAISS and FENSHSES for Nearest Neighbor Search in Hamming Space

* **arXiv ID:** `1906.10095`
* **Year:** `None`
* **Categories:** `['cs.LG cs.CV cs.IR stat.ML']`
* **URL:** https://arxiv.org/abs/1906.10095

### 3) Adversarial FDI Attack against AC State Estimation with ANN

* **arXiv ID:** `1906.11328`
* **Year:** `None`
* **Categories:** `['cs.LG cs.CR stat.ML']`
* **URL:** https://arxiv.org/abs/1906.11328

---

## LLM Answer

### 1. Short answer

The user query "FAISS ANN" refers to the FAISS library for approximate nearest neighbor (ANN) search. Paper 2 provides direct empirical comparisons of FAISS with FENSHSES for ANN search in Hamming space, while Paper 1 discusses a GPU-accelerated ANN search method (GGNN) that competes with FAISS in build-time and search speed. Paper 3 is less relevant as it focuses on adversarial attacks against artificial neural networks (not the FAISS library).

### 2. Most relevant papers

* **An Empirical Comparison of FAISS and FENSHSES for Nearest Neighbor Search in Hamming Space** (`1906.10095`)
  Directly compares FAISS (the library) with FENSHSES for ANN search in Hamming space, evaluating indexing speed, search latency, and RAM consumption.
  URL: https://arxiv.org/abs/1906.10095

* **GGNN: Graph-based GPU Nearest Neighbor Search** (`1912.01059`)
  Proposes a GPU-accelerated ANN search structure (GGNN) that competes with FAISS in build-time, accuracy, and search speed.
  URL: https://arxiv.org/abs/1912.01059

### 3. Suggested refined queries

1. `FAISS library approximate nearest neighbor search performance`
2. `Comparison of FAISS and FENSHSES for Hamming space nearest neighbor search`
3. `GPU-accelerated ANN search implementations`

---

## Notes

* This example highlights ambiguity handling (`ANN` can mean *Approximate Nearest Neighbor* or *Artificial Neural Network*).
* BM25 retrieves lexical matches.
* The LLM helps disambiguate relevance in the final synthesis.
